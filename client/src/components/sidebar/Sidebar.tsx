import NavbarRoutesData from "@/routes/navbar.route";
import React from "react";

const routes = [];

interface IProps {
  status: boolean;
}
const Sidebar = () => {
  return <div></div>;
};

export default Sidebar;
