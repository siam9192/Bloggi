import React from "react";

const Loading = () => {
  return (
    <div className="h-screen flex justify-center items-center">
      <img src="/images/logo.png" alt="" className="size-40 rounded-full animate-bounce" />
    </div>
  );
};

export default Loading;
