import React, { useEffect, useState } from "react";

interface IProps {
  // loading
}

const UseLoadingBounce = () => {
  const [isLoading, setIsLoading] = useState();

  useEffect(() => {});
};

export default UseLoadingBounce;
